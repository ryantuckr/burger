<h1>Burgers!</h1>

<h2>These burgers need to be eaten</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eaten already</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger to the list</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Has this been eaten?</label><br>
    <input type="radio" name="devour" value="1" checked> already devoured<br>
    <input type="radio" name="devour" value="0"> needs devouring
  </div>

  <button type="submit">Add Burger</button>
</form>